# 内置审计器

AgentGuard 提供了一组内置的后端审计能力，用于在运行结束后复核已存储的 trace 和 Agent 行为。

当前文档中单独说明的内置审计器能力有：

- [Agent 安全审计](../security_audit.md)：一个面向管理员的审计工作流，可以围绕用户、Session 和 Trace 的完整快照执行规则模式、大模型模式以及混合模式审计。

当你希望直接使用现成的审计工作流，而不是自己编写后端审计代码时，优先使用内置审计器。
